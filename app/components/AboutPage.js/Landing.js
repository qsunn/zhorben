import { s } from "@/app/styles"

export const Landing = () => {
    return <section className={s.container}>
        <div className="w-full">
            такая структура каждой секции, и тут уже ее контент
        </div>
    </section>
}